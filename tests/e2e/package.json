{
  "name": "@movies/e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E and API integration tests for the Movies app",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.16.0"
  }
}
